// 5 7 => 12
// -3 0 => -3 
// -7 12 => 5
//all input in functions comes in as a string

function add(a, b) {
	//convert a and b to numbers 
	// add a and b 
	a = parseFloat(a); 
	b = parseFloat(b);
	return a + b; 
}